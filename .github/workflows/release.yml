name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag (e.g. v1.2.0)'
        required: true
        default: 'v0.0.0-dev'

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          # macOS ARM64 (.dmg / .app)
          - os: macos-latest
            target: aarch64-apple-darwin
            args: '--config src-tauri/tauri.macos.conf.json'
            signing_identity: '-'

          # macOS x86_64 (.dmg / .app)
          - os: macos-latest
            target: x86_64-apple-darwin
            args: '--config src-tauri/tauri.macos.conf.json'
            signing_identity: '-'

          # Windows x64 (.exe / .msi)
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            args: ''
            signing_identity: ''

          # Windows ARM64 (.exe / .msi)
          - os: windows-latest
            target: aarch64-pc-windows-msvc
            args: ''
            signing_identity: ''

          # Linux x86_64 (.AppImage / .deb)
          - os: ubuntu-22.04
            target: x86_64-unknown-linux-gnu
            args: ''
            signing_identity: ''

          # Linux ARM64 (.AppImage / .deb) — 需要 GitHub Team/Enterprise
          - os: ubuntu-22.04-arm
            target: aarch64-unknown-linux-gnu
            args: ''
            signing_identity: ''

    runs-on: ${{ matrix.os }}

    env:
      TAG_NAME: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || inputs.tag_name }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install Linux system dependencies
        if: startsWith(matrix.os, 'ubuntu')
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libappindicator3-dev \
            librsvg2-dev \
            patchelf

      - name: Install frontend deps
        run: pnpm install

      - name: Install MSVC ARM64 toolchain (Windows ARM64 cross-compile)
        if: matrix.target == 'aarch64-pc-windows-msvc'
        shell: pwsh
        run: |
          $installer = "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vs_installer.exe"
          $vsPath = & "C:\Program Files (x86)\Microsoft Visual Studio\Installer\vswhere.exe" -latest -property installationPath
          & $installer modify --installPath $vsPath --add Microsoft.VisualStudio.Component.VC.Tools.ARM64 --quiet --norestart | Out-Null

      - name: Build and upload artifacts
        uses: tauri-apps/tauri-action@v0.5.20
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          APPLE_SIGNING_IDENTITY: ${{ matrix.signing_identity }}
        with:
          tauriScript: pnpm tauri
          args: --target ${{ matrix.target }} ${{ matrix.args }}
          tagName: ${{ env.TAG_NAME }}
          releaseName: 'Skills Manager ${{ env.TAG_NAME }}'
          releaseBody: 'See changelog for details.'
          releaseDraft: true
          prerelease: false

  publish:
    needs: build
    runs-on: ubuntu-latest
    env:
      TAG_NAME: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || inputs.tag_name }}
    steps:
      - name: Publish release
        run: gh release edit "$TAG_NAME" --draft=false --repo "$GITHUB_REPOSITORY"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
